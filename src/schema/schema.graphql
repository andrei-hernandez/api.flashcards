#Here create your Schema
type Query {  
  accountLogIn(userName: String, email: String, password: String!): credentialsFine!
  getFlashCards(usernName: String!, isLoggedIn: Boolean!): [FCard]
}

type Mutation {
  createUser(user: UserInput): User!
}

input UserInput {
  userName: String!
  email: String!
  password: String!
}

type FCard {
  id: ID!
  title: String!
  content: String!
  author: User!
  createdAt: String!
  editedAt: String
}

type User {
  id: ID!
  userName: String!
  email: String!
  password: String!
  flashCards: [FCard!]
}

scalar credentialsFine
scalar accountExists